# Run and deploy your CTH Osona app

This contains everything you need to run your app locally. It is built with Vanilla JavaScript, Tailwind CSS (v4), and Vite.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## GitHub Deployment
You can deploy this to GitHub Pages by pushing the `dist` folder generated by `npm run build`.
